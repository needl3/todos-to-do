import { login, logout, register, deleteAccount } from './auth'
import { addTodo, editTodo, checkTodo, syncTodo } from './todo'
import changeState from './changeState'
import { createPopup, removePopup } from './popup'

export {
    login,
    logout,
    register,
    deleteAccount,
    addTodo,
    editTodo,
    changeState,
    checkTodo,
    syncTodo,
    createPopup,
    removePopup,
}
